# Predavanje 1 â ReÄnici

## `d = {"k": "v"}`
## Read: `d[k]`, `d.get(k)`
## Create/Update: `d[k] = v`
## Delete: `del d[k]`, `d.pop(k)`
## `.items()`, `.keys()`, `.values()`

## Rezime
- KljuÄ-vrednost parovi
- `.get()` spreÄava KeyError