# Predavanje 1 â ReÄnici

## Kreiranje
`d = {"k": "v"}`

## CRUD
- Read: `d["k"]` ili `d.get("k", default)`
- Create: `d["novo"] = v`
- Delete: `del d["k"]` ili `d.pop("k")`

## Iteracija
`for k, v in d.items():`

## Rezime
- `.get()` za bezbedno Äitanje
- `.items()` za parove