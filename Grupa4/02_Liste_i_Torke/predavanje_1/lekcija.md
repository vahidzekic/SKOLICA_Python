# Predavanje 1 — Liste: Osnove

## Kreiranje
```python
gradovi = ["Beograd", "Novi Pazar", "Niš"]
```

## Indeksiranje
- `lista[0]` prvi, `lista[-1]` poslednji
- Slicing: `lista[start:stop:step]`

## CRUD Metode
| Metoda | Opis |
|---|---|
| `append(x)` | Dodaje na kraj |
| `insert(i,x)` | Umeće na poziciju |
| `remove(x)` | Briše prvu pojavu |
| `pop(i)` | Briše po indeksu |
| `sort()` | Sortira |

## Rezime
- Lista: `[]`, uređena, promenljiva
