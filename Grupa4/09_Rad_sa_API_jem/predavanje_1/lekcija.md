# Predavanje 1 â HTTP i requests

## HTTP: GET, POST, PUT, DELETE
## requests: `r = requests.get(url)`
## `.json()`, `.status_code`, `.ok`

## Rezime
- `requests.get()` za Äitanje
- `.json()` za parsiranje