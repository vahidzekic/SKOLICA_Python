# Predavanje 3 â Logging

## Nivoi: DEBUG < INFO < WARNING < ERROR < CRITICAL

## Konfiguracija
```
import logging
logging.basicConfig(level=logging.DEBUG)
```

## Rezime
- `logging` umesto `print()` u produkciji